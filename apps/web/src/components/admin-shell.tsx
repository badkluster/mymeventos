'use client';
import Image from 'next/image'; import Link from 'next/link'; import { usePathname, useRouter } from 'next/navigation'; import { LogOut, Moon, Sun } from 'lucide-react'; import { useTheme } from 'next-themes'; import { brandAssets } from '@/lib/brand-assets'; import { moduleForPath, userCanAccess, visibleAdminModules } from '@/lib/admin-permissions'; import { useSession } from './session-provider';
export function AdminShell({ children }: { children: React.ReactNode }) { const { user, logout } = useSession(); const router = useRouter(); const pathname = usePathname(); const { resolvedTheme, setTheme } = useTheme(); const items = visibleAdminModules(user); const currentModule = moduleForPath(pathname); const blocked = Boolean(currentModule && !userCanAccess(user, currentModule.permissions)); const isActive = (href: string) => href === '/admin' ? pathname === href : pathname === href || pathname.startsWith(`${href}/`); return <div className="min-h-screen bg-background text-foreground"><aside className="fixed inset-y-0 w-64 border-r bg-card p-5"><Link href="/admin" className="block" aria-label="Ir al panel de M&M Eventos"><Image src={brandAssets.logoDarkOnLight} alt="M&M Eventos" width={150} height={150} className="h-auto w-32 object-contain" priority /></Link><nav className="mt-10 space-y-1" aria-label="Módulos del backoffice">{items.map(({ href, label, icon: Icon }) => <Link key={href} href={href} aria-current={isActive(href) ? 'page' : undefined} className={`flex items-center gap-3 rounded-lg px-3 py-2 text-sm ${isActive(href) ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'}`}><Icon className="h-4 w-4" />{label}</Link>)}</nav></aside><main className="ml-64"><header className="flex h-16 items-center justify-between border-b px-8"><p className="text-sm text-muted">Administración</p><div className="flex items-center gap-3"><button aria-label="Cambiar tema" onClick={() => setTheme(resolvedTheme === 'dark' ? 'light' : 'dark')} className="rounded p-2 hover:bg-muted">{resolvedTheme === 'dark' ? <Sun className="h-4 w-4"/> : <Moon className="h-4 w-4"/>}</button><button aria-label="Cerrar sesión" onClick={() => void logout().then(() => router.push('/admin/login'))} className="flex items-center gap-2 text-sm"><span>{user?.firstName ?? user?.username}</span><LogOut className="h-4 w-4"/></button></div></header><div className="p-8">{blocked ? <section className="rounded-2xl border border-zinc-200 bg-white p-8 shadow-sm"><h1 className="text-xl font-semibold text-zinc-950">Sin acceso</h1><p className="mt-2 text-sm text-zinc-500">No tenés permisos para ver esta sección. Pedile a un administrador que revise tus permisos.</p></section> : children}</div></main></div>; }
