'use client';
import Link from 'next/link'; import { usePathname, useRouter } from 'next/navigation'; import { Bell, LayoutDashboard, LogOut, Moon, Settings, Sun, Users } from 'lucide-react'; import { useTheme } from 'next-themes'; import { useSession } from './session-provider';
const items = [{ href: '/admin', label: 'Panel', icon: LayoutDashboard }, { href: '/admin/users', label: 'Usuarios', icon: Users }, { href: '/admin/settings', label: 'Configuración', icon: Settings }];
export function AdminShell({ children }: { children: React.ReactNode }) { const { user, logout } = useSession(); const router = useRouter(); const pathname = usePathname(); const { resolvedTheme, setTheme } = useTheme(); return <div className="min-h-screen bg-background text-foreground"><aside className="fixed inset-y-0 w-64 border-r bg-card p-5"><Link href="/admin" className="text-xl font-bold">M&M <span className="text-muted">Eventos</span></Link><nav className="mt-10 space-y-1">{items.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={`flex items-center gap-3 rounded-lg px-3 py-2 text-sm ${pathname === href ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'}`}><Icon className="h-4 w-4" />{label}</Link>)}</nav></aside><main className="ml-64"><header className="flex h-16 items-center justify-between border-b px-8"><p className="text-sm text-muted">Administración</p><div className="flex items-center gap-3"><button aria-label="Cambiar tema" onClick={() => setTheme(resolvedTheme === 'dark' ? 'light' : 'dark')} className="rounded p-2 hover:bg-muted">{resolvedTheme === 'dark' ? <Sun className="h-4 w-4"/> : <Moon className="h-4 w-4"/>}</button><button aria-label="Notificaciones" className="rounded p-2 hover:bg-muted"><Bell className="h-4 w-4"/></button><button onClick={() => void logout().then(() => router.push('/admin/login'))} className="flex items-center gap-2 text-sm"><span>{user?.firstName ?? user?.username}</span><LogOut className="h-4 w-4"/></button></div></header><div className="p-8">{children}</div></main></div>; }
