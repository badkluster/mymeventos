'use client'; import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import { login } from '@/lib/auth';
export default function LoginPage() { const router = useRouter(); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const values = new FormData(event.currentTarget); const username = String(values.get('username') ?? ''); const password = String(values.get('password') ?? ''); if (!username || !password) return setError('Completá usuario y contraseña.'); setLoading(true); setError(''); try { await login(username, password); router.replace('/admin'); } catch (cause) { setError(cause instanceof Error ? cause.message : 'No se pudo iniciar sesión.'); } finally { setLoading(false); } } return <main className="grid min-h-screen place-items-center bg-zinc-950 p-5 text-white"><form onSubmit={submit} className="w-full max-w-sm space-y-5 rounded-2xl border border-zinc-700 bg-zinc-900 p-8"><div><p className="text-sm text-zinc-400">M&M Eventos</p><h1 className="mt-1 text-2xl font-semibold">Ingresar al backoffice</h1></div>{error && <p className="rounded bg-red-950 p-3 text-sm text-red-200">{error}</p>}<label className="grid gap-2 text-sm">Usuario<input name="username" className="rounded border border-zinc-700 bg-zinc-950 px-3 py-2" autoComplete="username" /></label><label className="grid gap-2 text-sm">Contraseña<input name="password" type="password" className="rounded border border-zinc-700 bg-zinc-950 px-3 py-2" autoComplete="current-password" /></label><button disabled={loading} className="w-full rounded bg-white px-4 py-2 font-medium text-black disabled:opacity-50">{loading ? 'Ingresando…' : 'Ingresar'}</button></form></main>; }
