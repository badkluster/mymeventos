'use client'; import { useRouter } from 'next/navigation'; import { useEffect } from 'react'; import { AdminShell } from '@/components/admin-shell'; import { SessionProvider, useSession } from '@/components/session-provider'; import { ThemeProvider } from '@/components/theme-provider';
function Protected({ children }: { children: React.ReactNode }) { const { loading, isAuthenticated } = useSession(); const router = useRouter(); useEffect(() => { if (!loading && !isAuthenticated) router.replace('/admin/login'); }, [loading, isAuthenticated, router]); if (loading || !isAuthenticated) return <main className="grid min-h-screen place-items-center">Cargando sesión…</main>; return <AdminShell>{children}</AdminShell>; }
export default function AdminLayout({ children }: { children: React.ReactNode }) { return <ThemeProvider><SessionProvider><Protected>{children}</Protected></SessionProvider></ThemeProvider>; }
