import { Router } from 'express'; import { z } from 'zod'; import { Permission } from '@mym/shared'; import { Salon } from './salon.model'; import { requireAuth, requirePermission } from '../../middlewares/auth'; import { validateRequest } from '../../middlewares/validateRequest'; import { asyncHandler } from '../../utils/asyncHandler'; import { sendSuccess } from '../../utils/api'; import { writeAuditLog } from '../audit/audit.service'; import { ApiError } from '../../middlewares/errorHandler'; import { idParams } from '../common.schemas'; import { getApiMessage } from '../../utils/messages';
const router = Router(); const createSchema = z.object({ body: z.object({ name: z.string().min(2), address: z.string().optional(), phone: z.string().optional(), email: z.string().email().optional(), active: z.boolean().optional() }), params: z.object({}), query: z.object({}) }); const updateSchema = z.object({ body: createSchema.shape.body.partial(), params: idParams.shape.params, query: z.object({}) }); router.use(requireAuth); router.get('/', requirePermission(Permission.SALONS_READ), asyncHandler(async (_r, res) => sendSuccess(res, { salons: await Salon.find({ deletedAt: null }).lean() }))); router.get('/:id', requirePermission(Permission.SALONS_READ), validateRequest(idParams), asyncHandler(async (req, res) => { const salon = await Salon.findOne({ _id: req.params.id, deletedAt: null }).lean(); if (!salon) throw new ApiError(404, 'SALON_NOT_FOUND'); return sendSuccess(res, { salon }); })); router.post('/', requirePermission(Permission.SALONS_UPDATE), validateRequest(createSchema), asyncHandler(async (req, res) => { const salon = await Salon.create({ ...req.body, createdBy: req.user!.id, updatedBy: req.user!.id }); await writeAuditLog(req, 'SALON_CREATE', 'Salon', salon._id.toString()); return sendSuccess(res, { salon }, 201, getApiMessage('SALON_CREATED')); })); router.patch('/:id', requirePermission(Permission.SALONS_UPDATE), validateRequest(updateSchema), asyncHandler(async (req, res) => { const salon = await Salon.findOneAndUpdate({ _id: req.params.id, deletedAt: null }, { ...req.body, updatedBy: req.user!.id }, { new: true }); if (!salon) throw new ApiError(404, 'SALON_NOT_FOUND'); await writeAuditLog(req, 'SALON_UPDATE', 'Salon', req.params.id); return sendSuccess(res, { salon }, 200, getApiMessage('SALON_UPDATED')); })); router.delete('/:id', requirePermission(Permission.SALONS_UPDATE), validateRequest(idParams), asyncHandler(async (req, res) => { const salon = await Salon.findOneAndUpdate({ _id: req.params.id, deletedAt: null }, { deletedAt: new Date(), deletedBy: req.user!.id }, { new: true }); if (!salon) throw new ApiError(404, 'SALON_NOT_FOUND'); await writeAuditLog(req, 'SALON_DELETE', 'Salon', req.params.id); return sendSuccess(res, { deleted: true }, 200, getApiMessage('SALON_DELETED')); })); export default router;
