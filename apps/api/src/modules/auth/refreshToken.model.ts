import { Schema, model, models } from 'mongoose';
const refreshTokenSchema = new Schema({ userId: { type: Schema.Types.ObjectId, ref: 'User', required: true, index: true }, tokenHash: { type: String, required: true, unique: true }, expiresAt: { type: Date, required: true, index: { expires: 0 } }, revokedAt: Date, replacedByTokenHash: String, createdByIp: String, userAgent: String, channel: { type: String, enum: ['web', 'mobile'], default: 'web', index: true }, installationId: { type: String, index: true } }, { timestamps: true });
export const RefreshToken = models.RefreshToken || model('RefreshToken', refreshTokenSchema);
