import { Router } from 'express'; import { z } from 'zod'; import { Permission, Role } from '@mym/shared'; import { User } from './user.model'; import { requireAuth, requirePermission } from '../../middlewares/auth'; import { validateRequest } from '../../middlewares/validateRequest'; import { asyncHandler } from '../../utils/asyncHandler'; import { sendSuccess } from '../../utils/api'; import { hashPassword } from '../../utils/password'; import { writeAuditLog } from '../audit/audit.service'; import { ApiError } from '../../middlewares/errorHandler'; import { idParams } from '../common.schemas'; import { getApiMessage } from '../../utils/messages';
const router = Router(); const roles = z.nativeEnum(Role); const permissions = z.nativeEnum(Permission); const createSchema = z.object({ body: z.object({ username: z.string().min(3), email: z.string().email(), password: z.string().min(12), firstName: z.string().min(1), lastName: z.string().min(1), phone: z.string().optional(), avatarUrl: z.string().url().optional(), roles: z.array(roles).min(1).optional(), permissionOverrides: z.array(permissions).optional(), salonIds: z.array(z.string().regex(/^[0-9a-fA-F]{24}$/)).optional(), active: z.boolean().optional() }), params: z.object({}), query: z.object({}) }); const updateSchema = z.object({ body: createSchema.shape.body.omit({ username: true, email: true, password: true }).partial(), params: idParams.shape.params, query: z.object({}) });
router.use(requireAuth); router.get('/', requirePermission(Permission.USERS_READ), asyncHandler(async (_request, response) => sendSuccess(response, { users: await User.find({ deletedAt: null }).select('-passwordHash').lean() }))); router.get('/:id', requirePermission(Permission.USERS_READ), validateRequest(idParams), asyncHandler(async (request, response) => { const user = await User.findOne({ _id: request.params.id, deletedAt: null }).select('-passwordHash').lean(); if (!user) throw new ApiError(404, 'USER_NOT_FOUND'); return sendSuccess(response, { user }); }));
router.post('/', requirePermission(Permission.USERS_CREATE), validateRequest(createSchema), asyncHandler(async (request, response) => { const { password, ...input } = request.body; const user = await User.create({ ...input, username: input.username.toLowerCase(), email: input.email.toLowerCase(), passwordHash: await hashPassword(password), createdBy: request.user!.id, updatedBy: request.user!.id }); await writeAuditLog(request, 'USER_CREATE', 'User', user._id.toString()); const safeUser = user.toObject() as Record<string, unknown>; delete safeUser.passwordHash; return sendSuccess(response, { user: safeUser }, 201, getApiMessage('USER_CREATED')); }));
router.patch('/:id', requirePermission(Permission.USERS_UPDATE), validateRequest(updateSchema), asyncHandler(async (request, response) => { const user = await User.findOneAndUpdate({ _id: request.params.id, deletedAt: null }, { ...request.body, updatedBy: request.user!.id }, { new: true }).select('-passwordHash').lean(); if (!user) throw new ApiError(404, 'USER_NOT_FOUND'); await writeAuditLog(request, 'USER_UPDATE', 'User', request.params.id, { roleOrPermissionChanged: Boolean(request.body.roles || request.body.permissionOverrides) }); return sendSuccess(response, { user }, 200, getApiMessage('USER_UPDATED')); }));
router.delete('/:id', requirePermission(Permission.USERS_DELETE), validateRequest(idParams), asyncHandler(async (request, response) => { const user = await User.findOneAndUpdate({ _id: request.params.id, deletedAt: null }, { deletedAt: new Date(), deletedBy: request.user!.id, active: false }, { new: true }); if (!user) throw new ApiError(404, 'USER_NOT_FOUND'); await writeAuditLog(request, 'USER_DELETE', 'User', request.params.id); return sendSuccess(response, { deleted: true }, 200, getApiMessage('USER_DELETED')); }));
for (const [path, active] of [['/:id/activate', true], ['/:id/deactivate', false]] as const) router.patch(path, requirePermission(Permission.USERS_UPDATE), validateRequest(idParams), asyncHandler(async (request, response) => { const user = await User.findOneAndUpdate({ _id: request.params.id, deletedAt: null }, { active, updatedBy: request.user!.id }, { new: true }).select('-passwordHash').lean(); if (!user) throw new ApiError(404, 'USER_NOT_FOUND'); await writeAuditLog(request, active ? 'USER_ACTIVATE' : 'USER_DEACTIVATE', 'User', request.params.id); return sendSuccess(response, { user }, 200, getApiMessage('USER_UPDATED')); })); export default router;
