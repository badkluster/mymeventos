import { Router } from 'express';
import { z } from 'zod';
import { Permission } from '@mym/shared';
import { requireAuth, requirePermission } from '../../middlewares/auth';
import { validateRequest } from '../../middlewares/validateRequest';
import { asyncHandler } from '../../utils/asyncHandler';
import { sendSuccess } from '../../utils/api';
import { ApiError } from '../../middlewares/errorHandler';
import { writeAuditLog } from '../audit/audit.service';
import { DigitalTicket, TicketAccessAttempt, TicketOrder, TicketPublication, TicketType } from './ticket.models';
import { claimTicketCheckIn, expirePendingOrders, markOrderPaid, releaseOrderReservation, reservePublicOrder, ticketPublicView } from './ticket.service';
const id = z.string().regex(/^[0-9a-fA-F]{24}$/); const slug = z.string().trim().toLowerCase().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/).max(100); const publicToken = z.string().regex(/^[A-Za-z0-9_-]{24,}$/);
const publicationBody = z.object({ title: z.string().trim().min(2).max(180), slug, description: z.string().max(5000).optional(), category: z.string().max(80).optional(), coverImage: z.string().url().optional().or(z.literal('')), gallery: z.array(z.string().url()).max(12).optional(), startsAt: z.coerce.date(), endsAt: z.coerce.date().optional(), venueName: z.string().max(200).optional(), address: z.string().max(300).optional(), city: z.string().max(100).optional(), province: z.string().max(100).optional(), mapsUrl: z.string().url().optional().or(z.literal('')), accessInfo: z.string().max(4000).optional(), restrictions: z.string().max(4000).optional(), minimumAge: z.coerce.number().int().min(0).max(120).optional(), termsAndConditions: z.string().max(10000).optional(), cancellationPolicy: z.string().max(4000).optional(), refundPolicy: z.string().max(4000).optional(), salesOpenAt: z.coerce.date().optional(), salesCloseAt: z.coerce.date().optional(), capacity: z.coerce.number().int().min(0), maxTicketsPerOrder: z.coerce.number().int().min(1).max(50).optional(), status: z.enum(['draft', 'scheduled', 'active', 'paused', 'sold_out', 'closed', 'cancelled', 'archived']).optional(), allowFreeTickets: z.boolean().optional(), paymentConfig: z.object({ enabled: z.boolean().optional(), provider: z.enum(['mercado_pago', 'manual', 'none']).optional(), reservationMinutes: z.coerce.number().int().min(1).max(120).optional(), feePayer: z.enum(['organizer', 'buyer']).optional() }).optional(), qrConfig: z.object({ allowReentry: z.boolean().optional(), maxAccesses: z.coerce.number().int().min(1).optional(), validFrom: z.coerce.date().optional(), validUntil: z.coerce.date().optional(), allowRevert: z.boolean().optional() }).optional() });
const typeBody = z.object({ name: z.string().trim().min(1).max(120), description: z.string().max(1000).optional(), price: z.coerce.number().min(0), capacity: z.coerce.number().int().min(0), currency: z.string().length(3).optional(), maxPerOrder: z.coerce.number().int().min(1).max(50).optional(), salesOpenAt: z.coerce.date().optional(), salesCloseAt: z.coerce.date().optional(), status: z.enum(['active', 'inactive']).optional(), displayOrder: z.coerce.number().int().min(0).optional(), isFree: z.boolean().optional(), isCourtesy: z.boolean().optional(), attendeeFields: z.array(z.string().max(50)).max(10).optional() });
const orderBody = z.object({ buyer: z.object({ name: z.string().trim().min(2).max(160), email: z.string().trim().email(), phone: z.string().trim().max(40).optional(), documentNumber: z.string().trim().max(50).optional() }), selections: z.array(z.object({ ticketTypeId: id, quantity: z.coerce.number().int().min(1).max(50) })).min(1).max(20), idempotencyKey: z.string().trim().min(12).max(200), expiresInMinutes: z.coerce.number().int().min(1).max(120).optional() });
const checkInBody = z.object({ token: publicToken, accessPoint: z.string().trim().max(120).optional(), idempotencyKey: z.string().trim().min(8).max(200).optional() });
const schema = (body: z.ZodTypeAny, params: z.ZodRawShape) => z.object({ body, params: z.object(params), query: z.object({}).passthrough() }); const admin = Router(); const publicRouter = Router();
async function publicationForUser(publicationId: string) { const publication: any = await TicketPublication.findOne({ _id: publicationId, deletedAt: null }).lean(); if (!publication) throw new ApiError(404, 'TICKET_PUBLICATION_NOT_FOUND', 'La publicación no existe.'); return publication; }
admin.use(requireAuth);
admin.get('/publications', requirePermission(Permission.TICKETS_READ), asyncHandler(async (_req, res) => sendSuccess(res, { publications: await TicketPublication.find({ deletedAt: null }).sort({ createdAt: -1 }).lean() })));
admin.post('/publications', requirePermission(Permission.TICKETS_CREATE), validateRequest(schema(publicationBody, {})), asyncHandler(async (req, res) => { const publication = await TicketPublication.create({ ...req.body, createdBy: req.user!.id, updatedBy: req.user!.id }); await writeAuditLog(req, 'ticket_publication_created', 'TicketPublication', String(publication._id)); return sendSuccess(res, { publication }, 201); }));
admin.get('/publications/:publicationId', requirePermission(Permission.TICKETS_READ), validateRequest(schema(z.unknown().optional(), { publicationId: id })), asyncHandler(async (req, res) => { const publication = await publicationForUser(req.params.publicationId); await expirePendingOrders(String(publication._id)); const types = await TicketType.find({ publicationId: publication._id, deletedAt: null }).sort({ displayOrder: 1, createdAt: 1 }).lean(); return sendSuccess(res, { publication, types }); }));
admin.patch('/publications/:publicationId', requirePermission(Permission.TICKETS_UPDATE), validateRequest(schema(publicationBody.partial().refine((v) => Object.keys(v).length > 0), { publicationId: id })), asyncHandler(async (req, res) => { const publication = await publicationForUser(req.params.publicationId); const used = publication.soldCount + publication.reservedCount; if (req.body.capacity !== undefined && req.body.capacity < used) throw new ApiError(409, 'TICKET_CAPACITY_TOO_LOW', 'El cupo no puede ser menor a las entradas reservadas o vendidas.'); const updated = await TicketPublication.findByIdAndUpdate(publication._id, { $set: { ...req.body, updatedBy: req.user!.id } }, { new: true }); await writeAuditLog(req, 'ticket_publication_updated', 'TicketPublication', String(publication._id), req.body); return sendSuccess(res, { publication: updated }); }));
admin.post('/publications/:publicationId/types', requirePermission(Permission.TICKETS_UPDATE), validateRequest(schema(typeBody, { publicationId: id })), asyncHandler(async (req, res) => { const publication = await publicationForUser(req.params.publicationId); if (req.body.capacity > publication.capacity) throw new ApiError(400, 'TICKET_TYPE_CAPACITY_INVALID', 'El cupo no puede superar el general.'); const ticketType = await TicketType.create({ ...req.body, publicationId: publication._id, createdBy: req.user!.id, updatedBy: req.user!.id }); return sendSuccess(res, { ticketType }, 201); }));
admin.patch('/publications/:publicationId/types/:typeId', requirePermission(Permission.TICKETS_UPDATE), validateRequest(schema(typeBody.partial().refine((v) => Object.keys(v).length > 0), { publicationId: id, typeId: id })), asyncHandler(async (req, res) => { await publicationForUser(req.params.publicationId); const ticketType: any = await TicketType.findOne({ _id: req.params.typeId, publicationId: req.params.publicationId, deletedAt: null }); if (!ticketType) throw new ApiError(404, 'TICKET_TYPE_NOT_FOUND'); if (req.body.capacity !== undefined && req.body.capacity < ticketType.soldCount + ticketType.reservedCount) throw new ApiError(409, 'TICKET_CAPACITY_TOO_LOW'); Object.assign(ticketType, req.body, { updatedBy: req.user!.id }); await ticketType.save(); return sendSuccess(res, { ticketType }); }));
admin.get('/publications/:publicationId/orders', requirePermission(Permission.TICKETS_READ), validateRequest(schema(z.unknown().optional(), { publicationId: id })), asyncHandler(async (req, res) => { await publicationForUser(req.params.publicationId); await expirePendingOrders(req.params.publicationId); return sendSuccess(res, { orders: await TicketOrder.find({ publicationId: req.params.publicationId, deletedAt: null }).sort({ createdAt: -1 }).lean() }); }));
admin.post('/orders/:orderId/mark-paid', requirePermission(Permission.TICKETS_UPDATE), validateRequest(schema(z.object({ method: z.enum(['cash', 'bank_transfer', 'mercado_pago', 'card', 'other']), reference: z.string().trim().max(160).optional() }), { orderId: id })), asyncHandler(async (req, res) => { const order: any = await TicketOrder.findOne({ _id: req.params.orderId, deletedAt: null }); if (!order) throw new ApiError(404, 'TICKET_ORDER_NOT_FOUND'); await publicationForUser(String(order.publicationId)); return sendSuccess(res, { order: await markOrderPaid(order, { ...req.body, userId: req.user!.id }) }); }));
admin.post('/orders/:orderId/cancel', requirePermission(Permission.TICKETS_UPDATE), validateRequest(schema(z.object({}).optional(), { orderId: id })), asyncHandler(async (req, res) => { const order: any = await TicketOrder.findOne({ _id: req.params.orderId, deletedAt: null }); if (!order) throw new ApiError(404, 'TICKET_ORDER_NOT_FOUND'); await publicationForUser(String(order.publicationId)); if (!await releaseOrderReservation(order, 'cancelled')) throw new ApiError(409, 'TICKET_ORDER_NOT_CANCELLABLE'); return sendSuccess(res, { cancelled: true }); }));
admin.post('/publications/:publicationId/check-in', requirePermission(Permission.TICKETS_VALIDATE), validateRequest(schema(checkInBody, { publicationId: id })), asyncHandler(async (req, res) => { await publicationForUser(req.params.publicationId); const payload = req.body; if (payload.idempotencyKey) { const prior: any = await TicketAccessAttempt.findOne({ publicationId: req.params.publicationId, action: 'check_in', idempotencyKey: payload.idempotencyKey }).lean(); if (prior) return sendSuccess(res, { result: prior.result, idempotent: true }); } const ticket: any = await claimTicketCheckIn(req.params.publicationId, payload.token, req.user!.id, payload.accessPoint); const existing: any = !ticket ? await DigitalTicket.findOne({ publicationId: req.params.publicationId, publicToken: payload.token, deletedAt: null }).lean() : null; const result = ticket ? 'valid' : existing?.status === 'used' ? 'used' : 'invalid'; await TicketAccessAttempt.create({ ticketId: ticket?._id ?? existing?._id, publicationId: req.params.publicationId, operatorUserId: req.user!.id, action: 'check_in', result, accessPoint: payload.accessPoint, idempotencyKey: payload.idempotencyKey }); return sendSuccess(res, { result, ticket: ticket ?? existing }); }));
admin.post('/publications/:publicationId/check-in/revert', requirePermission(Permission.TICKETS_UPDATE), validateRequest(schema(checkInBody, { publicationId: id })), asyncHandler(async (req, res) => { const publication = await publicationForUser(req.params.publicationId); if (!publication.qrConfig?.allowRevert) throw new ApiError(409, 'TICKET_REVERT_DISABLED'); const ticket: any = await DigitalTicket.findOneAndUpdate({ publicationId: publication._id, publicToken: req.body.token, status: 'used', deletedAt: null }, { $set: { status: 'valid', validatedAt: null, validatedByUserId: null, updatedBy: req.user!.id } }, { new: true }); if (!ticket) throw new ApiError(409, 'TICKET_NOT_USED'); await TicketAccessAttempt.create({ ticketId: ticket._id, publicationId: publication._id, operatorUserId: req.user!.id, action: 'revert', result: 'reverted', accessPoint: req.body.accessPoint, idempotencyKey: req.body.idempotencyKey }); return sendSuccess(res, { ticket }); }));
publicRouter.get('/tickets/:slug', validateRequest(schema(z.unknown().optional(), { slug })), asyncHandler(async (req, res) => { await expirePendingOrders(); const publication: any = await TicketPublication.findOne({ slug: req.params.slug, deletedAt: null }).lean(); if (!publication || publication.status !== 'active') throw new ApiError(404, 'TICKET_PUBLICATION_NOT_FOUND'); const types = await TicketType.find({ publicationId: publication._id, status: 'active', deletedAt: null }).sort({ displayOrder: 1 }).lean(); return sendSuccess(res, { publication: { ...publication, availableCount: Math.max(0, publication.capacity - publication.reservedCount - publication.soldCount) }, types: types.map((type: any) => ({ ...type, availableCount: Math.max(0, type.capacity - type.reservedCount - type.soldCount) })) }); }));
publicRouter.post('/tickets/:slug/orders', validateRequest(schema(orderBody, { slug })), asyncHandler(async (req, res) => { const publication: any = await TicketPublication.findOne({ slug: req.params.slug, deletedAt: null }); if (!publication) throw new ApiError(404, 'TICKET_PUBLICATION_NOT_FOUND'); const result = await reservePublicOrder({ publication, ...req.body }); return sendSuccess(res, { order: result.order, reused: result.reused }, result.reused ? 200 : 201); }));
publicRouter.get('/ticket/:token', validateRequest(schema(z.unknown().optional(), { token: publicToken })), asyncHandler(async (req, res) => sendSuccess(res, await ticketPublicView(req.params.token))));
export { admin as ticketRoutes, publicRouter as publicTicketRoutes };
