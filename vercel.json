{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd \"$(git rev-parse --show-toplevel)\" && corepack enable && corepack pnpm@10.24.0 install --frozen-lockfile",
  "buildCommand": "cd \"$(git rev-parse --show-toplevel)\" && corepack pnpm@10.24.0 --filter @mym/api build && corepack pnpm@10.24.0 --filter @mym/web build",
  "devCommand": "pnpm dev",
  "outputDirectory": "../web/.next",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30,
      "includeFiles": "apps/web/public/brand/mym-logo-dark-on-light.jpg"
    }
  },
  "rewrites": [
    {
      "source": "/health",
      "destination": "/api/health"
    }
  ]
}
